const { NavLink } = require("react-router-dom");

function Layout() {
    return (
        <div>
            <nav>
            <NavLink />
            <NavLink/>
            </nav>
        </div>
    )
}

export default Layout